{
  "name": "LiquidityTree",
  "version": "1.0.0",
  "description": "segment tree solidity library",
  "main": "index.js",
  "author": "Kiselev Maksim",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.15",
    "hardhat-contract-sizer": "^2.10.0",
    "prettier-plugin-solidity": "^1.4.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  },
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "prettier_sol": "npx prettier --write --plugin=prettier-plugin-solidity \"contracts/**/*.sol\"",
    "prettier-test": "npx prettier --write {test,utils}/*.js --print-width 120 --tab-width 2"
  }
}
